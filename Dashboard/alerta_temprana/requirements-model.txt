# Dependencias OPCIONALES para puntuar en línea con el modelo empaquetado dentro del tablero
# (sin depender de la API). El pickle del Random Forest ocupa ~550 MB en RAM.
# Instalar desde la raíz del repositorio:
#   pip install -r Dashboard/alerta_temprana/requirements-model.txt
streamlit==1.64.0
plotly>=6.0,<7
Pillow
openpyxl
requests
scikit-learn==1.3.2
numpy>=1.23,<2.0
pandas>=2.0,<3.0
./Despliegue/model_morosidad-0.0.1-py3-none-any.whl
